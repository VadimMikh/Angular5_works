export class ProductList {
    constructor(public id: number, public name: string, public price: number, public category: number) {  }
}
export class Range {
    constructor(public min: number, public  max: number) {  }
}